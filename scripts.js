(() => { const slides=[...document.querySelectorAll('.slide')]; const toggle=document.querySelector('[data-pause]'); let i=0, paused=matchMedia('(prefers-reduced-motion: reduce)').matches; function show(n){slides.forEach((s,x)=>s.classList.toggle('active',x===n));} if(slides.length){show(0);setInterval(()=>{if(!paused){i=(i+1)%slides.length;show(i)}},5500); document.querySelector('[data-prev]')?.addEventListener('click',()=>{i=(i+slides.length-1)%slides.length;show(i)});document.querySelector('[data-next]')?.addEventListener('click',()=>{i=(i+1)%slides.length;show(i)});toggle?.addEventListener('click',()=>{paused=!paused;toggle.textContent=paused?'Play carousel':'Pause carousel';});}})();
